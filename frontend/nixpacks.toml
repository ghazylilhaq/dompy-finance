# Nixpacks configuration for NeoBudget Frontend
# This file is optional - nixpacks auto-detects Next.js projects
# Included for explicit control and documentation

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

