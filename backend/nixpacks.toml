# Nixpacks configuration for NeoBudget Backend
# This file is optional - nixpacks auto-detects Python projects
# Included for explicit control and documentation

[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
# No build phase needed for Python

[start]
cmd = "./start.sh"

